extends ../layout

block content

  .row
    .col-xs-12
      h1 Please select a Pod type
      .row
        .col-xs-12
          h3 Pods for each Machine Type
        - each type in product_types
          .col-xs-4
            h4 #{type.title}
            p These fit the #{type.title} Machine
            p
              a.btn.btn-default(href="/pods/type/" + type._id) Detail
      .row
        .col-xs-12
          h3 Pod Flavors
        - each flavor in flavor_list
          .col-xs-4
            h4 #{flavor.title}
            p #{flavor.title} is the best flavor we have...
            p
              a.btn.btn-default(href="/pods/flavor/" + flavor._id) Detail
              
      .row
        .col-xs-12
          h3 Pack Sizes
        - each pack_size in pack_size_list
          .col-xs-4
            h4 #{pack_size.size} Dozen
            p #{pack_size.size} Dozen is the perfect size for you
            p
              a.btn.btn-default(href="/pods/size/" + pack_size._id) Detail 
